# 프로그램 개요서 (Program Overview)
## 문서 기반 RAG Q&A 앱

---

### 문서 정보

| 항목 | 내용 |
|---|---|
| 문서명 | 문서 기반 RAG Q&A 앱 — 프로그램 개요서 |
| 작성 목적 | 제품의 비전·목표·범위 정의 및 이해관계자 공감대 형성 |
| 관련 자료 | Codex CLI로 배우는 바이브 코딩 1일 커리큘럼 |
| 버전 | v0.1 (Draft) |

---

## 1. 배경 (Background)

조직 내·외부에는 PDF, Word, HTML 등 다양한 형식의 문서가 흩어져 있으며, 필요한 정보를 찾기 위해 문서를 직접 열어 검색하는 데 많은 시간이 소요됩니다. 특히 표나 레이아웃이 복잡한 문서일수록 단순 텍스트 검색(Ctrl+F)이나 OCR 기반 검색으로는 정확한 정보를 찾기 어렵습니다.

본 프로그램은 이러한 문제를 해결하기 위해, 문서의 구조(제목·표·단락)를 보존한 채 내용을 이해하고, 사용자의 자연어 질문에 문서 근거를 기반으로 답변하는 RAG(Retrieval-Augmented Generation) 기반 Q&A 애플리케이션을 만드는 것을 목표로 합니다.

---

## 2. 비전 (Vision)

> **"문서를 열어보지 않아도, 묻기만 하면 답을 찾아주는 AI"**

사용자가 PDF·Word·HTML 문서를 업로드 하면, AI가 문서의 구조와 맥락을 정확히 파악하고, 자연어 질문에 대해 신뢰할 수 있는 근거(출처)와 함께 답변을 제공하는 환경을 구축합니다.

---

## 3. 무엇을 만드는가 (What)

**문서 기반 RAG Q&A 앱**: 사용자가 문서를 업로드하면 AI가 자동으로 내용을 파싱·색인화하고, 채팅 형태로 질문하면 문서에 근거한 답변과 출처(문서명·페이지)를 함께 제시하는 웹 애플리케이션.

### 핵심 기능

- **문서 업로드 및 파싱**: PDF·Word·HTML 문서를 업로드하면 표·레이아웃 구조를 보존한 형태로 변환
- **지능형 검색(RAG)**: 질문과 관련된 문서 조각을 찾아 근거 기반 답변 생성
- **답변 신뢰도 검증**: 생성된 답변이 실제 문서 내용에 부합하는지 자동 검증, 미흡 시 재시도
- **대화형 Q&A 화면**: 질문 입력 → 답변 스트리밍 → 출처 표시 → 대화 이력 관리
- **문서 관리 화면**: 업로드 진행 상태 확인, 색인 완료 문서 목록 조회 및 삭제

---

## 4. 왜 만드는가 (Why)

| 문제 (Pain Point) | 해결 방안 |
|---|---|
| 문서가 많아 필요한 정보를 찾기 어려움 | 자연어 질문으로 즉시 답변 제공 |
| 단순 OCR/텍스트 추출은 표·레이아웃 구조를 깨뜨림 | 구조를 보존하는 문서 파싱으로 정확도 향상 |
| AI 답변의 사실관계(hallucination)를 신뢰하기 어려움 | 답변 근거를 문서 출처와 함께 명시하고, 근거 부합 여부를 자동 검증 |
| 검색 결과가 부정확할 때 재질문이 번거로움 | 검색·답변 품질을 자동 평가하고 필요 시 스스로 재시도하는 자기 교정형(Self-Correcting) 구조 |

---

## 5. 범위 (Scope)

### 포함 범위 (In-Scope)

- PDF, Word, HTML 형식의 문서 업로드 및 파싱
- 문서 내용의 벡터화 및 색인(임베딩 기반 검색)
- 자연어 질문에 대한 근거 기반 답변 생성
- 답변의 문서 근거 부합 여부 자동 검증 및 재시도
- 문서 관리 화면 (업로드·색인·삭제)
- Q&A 채팅 화면 (질문·답변·출처·대화 이력)
- REST API 기반 백엔드 (문서 등록/조회/삭제, 질의응답)

### 제외 범위 (Out-of-Scope)

- 이미지 전용 문서(스캔본 등)의 별도 OCR 고도화
- 다중 사용자 권한 관리, 조직/팀 단위 협업 기능
- 영구 데이터베이스 기반 영속 저장 (1차 버전은 인메모리 벡터 스토어 기준)
- 다국어 지원 확장 (1차 버전은 한국어 특화 임베딩 기준)
- 모바일 앱 별도 개발

> 위 제외 항목은 1일 스프린트로 진행되는 1차 버전의 범위이며, 향후 확장 단계에서 재검토할 수 있습니다.

---

## 6. 핵심 사용자 흐름 (Key User Flow)

```
문서 업로드 (PDF/Word/HTML)
      ↓
문서 구조 파싱 (표·레이아웃 보존)
      ↓
내용 벡터화 및 색인
      ↓
사용자 질문 입력
      ↓
관련 문서 조각 검색 → 답변 생성 → 근거 검증
      ↓
답변 + 출처(문서명·페이지) 표시
```

---

## 7. 주요 기술 스택 (High-level)

| 영역 | 기술 |
|---|---|
| 백엔드 / 에이전트 오케스트레이션 | Python, LangGraph |
| 프론트엔드 | React, Vite, TailwindCSS |
| 문서 파싱 | Upstage Document Parse |
| 임베딩 | Solar Embedding (한국어 특화) |
| 답변 생성 | Solar Pro 3 |
| 답변 신뢰도 검증 | Groundness Check |

> 본 개요서는 "무엇을, 왜 만드는가"에 초점을 맞춘 문서로, 구체적인 아키텍처·API 명세·구현 방법은 별도의 PRD(Product Requirement Document) 문서에서 다룹니다.

---

## 8. 기대 효과 (Expected Outcomes)

- 문서를 직접 검토하지 않고도 필요한 정보를 빠르게 확인할 수 있는 경험 제공
- 출처가 명시된 답변을 통해 AI 답변에 대한 신뢰도 향상
- RAG 파이프라인의 핵심 구조(파싱 → 임베딩 → 생성 → 검증)를 체험할 수 있는 레퍼런스 애플리케이션 확보

---

## 9. 이해관계자 (Stakeholders)

| 역할 | 관심사 |
|---|---|
| 최종 사용자 | 정확하고 신뢰할 수 있는 문서 기반 답변 |
| 개발/학습자 | Codex CLI 기반 바이브 코딩으로 RAG 앱을 직접 구축하는 경험 |
| 의사결정자/스폰서 | 짧은 기간 내 작동하는 프로토타입(PoC)으로 가능성 검증 |

---

## 10. 다음 단계 (Next Steps)

본 프로그램 개요서에 대한 합의가 이루어지면, 다음 문서로 구체화를 진행합니다.

1. 기능 요구사항 정의서 (Functional Requirements)
2. 화면/API 설계 문서
3. 개발 일정 및 작업 분해 (WBS)

---

*본 문서는 초안(Draft)이며, 이해관계자 검토 후 확정됩니다.*
